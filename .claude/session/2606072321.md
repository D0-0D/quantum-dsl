# Session 2606072321 — PDF-to-AI reference conversion

## Goal
Convert `refer/2511.10479v1.pdf` into AI-readable reference material, with formula/layout fidelity prioritized and images ignored.

## Locked decisions
- Project journaling uses `.claude/`; there is no `.codex/` directory in this repo.
- Preserve formulas and displayed math by extracting one PDF page at a time with `pdftotext -layout -enc UTF-8`.
- Keep each full-text page in a fenced `text` block in Markdown so line breaks, indentation, symbols, and matrices are not reflowed by Markdown.
- Images are omitted; figure captions remain where they exist in the PDF text layer.

## Work done
- Generated `refer/2511.10479v1.layout.txt` as raw layout-preserving text with explicit page separators.
- Generated `refer/2511.10479v1.md` as full AI lookup Markdown with PDF outline and 143 page sections.
- Generated `refer/2511.10479v1.index.md` with metadata, conversion notes, cleaned abstract, search terms, PDF outline, and suggested `rg` lookup commands.
- Removed temporary single-page extraction test files.

## Verification
- `pdfinfo refer\2511.10479v1.pdf` reports 143 PDF pages.
- Generation script wrote 143 pages to the Markdown/reference set.
- `rg -n "^## Page " refer\2511.10479v1.md` found page headings from `Page 1` through `Page 143`.
- Spot-checked Hamiltonian equations around Eq. (27)–(32); symbols and multi-line indentation are preserved in the fenced text block.

## Open issues / next steps
- This conversion preserves the PDF text layer, not original LaTeX source. Exact TeX equations would require adding the arXiv source package to `refer/` and converting from source.

## Resolution — arXiv source conversion
- User provided `refer/arXiv-2511.10479v1.tar.gz`; extracted it to `refer/2511.10479v1.source/`.
- Generated `refer/2511.10479v1.source.md` from the TeX bundle using `pandoc` and `QDA.bib`.
- Preserved TeX math in the source-derived Markdown; verified 51 `\ket` occurrences, 50 `\begin{align}` blocks, and 43 `\begin{equation}` blocks remain.
- Added Markdown references to all 13 extracted figure PDFs under `refer/2511.10479v1.source/figs/`; verified 13 unique figure targets and no missing paths.
- Updated `refer/2511.10479v1.index.md` so the source-derived Markdown is primary and the earlier PDF-layout Markdown remains a fallback.
- Fixed citation-key case mismatch: `QDA.tex` cited `Abadi2016Tensorflow`, while `QDA.bib` defines `Abadi2016TensorFlow`; normalized the TeX/source-conversion copy and regenerated `refer/2511.10479v1.source.md`.
- Verified the TensorFlow citation now resolves to Abadi et al. 2016, with no unresolved `Abadi2016Tensorflow?` marker remaining.
