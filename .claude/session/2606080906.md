# Session 2606080906 — Post-PR#14 docs/examples cleanup + pipeline re-verify

**Goal** (user, ultracode): for the work completed in `plan.md` (PR #14, native-geo phase,
M1/M3/M5a/M6/M7), refresh examples + README, run the whole pipeline end-to-end, install deps;
clean up ALL old v3 examples (incl. `examples/dsl/.note/`); add `./refer` to `.gitignore`.

## Locked decisions (user)
- **Cleanup depth = examples + v3 docs only.** The v3 *engine* stays (emit_geo / M5a reuses
  `build_ir` + `dsl_templates/`). Removed only the v3 *showcase* material + v3 design notes; kept
  legacy `build_design`→QDesign code and ALL 5 legacy test files (`test_design_dsl*.py`) green.
- **Top-level**: keep `gmsh_dsl_notes.md`; `rm` the local gmsh SDK download
  (`gmsh-4.15.2-Linux64/` + `.tgz`, ~38 MB, was gitignored/untracked — disk only).

## Work done
- **Relocated** the 2 test-referenced fixtures via `git mv`:
  `examples/dsl/yaml/{chain_2q_native,transmon_pocket_2q}.metal.yaml` → `tests/fixtures/`.
  Updated `EXAMPLE =` paths in `test_design_dsl.py`, `test_design_dsl_gmsh.py`,
  `test_design_dsl_transmon_pocket.py` (`parents[1]/examples/dsl/yaml/` → `parent/fixtures/`).
- **Deleted (git rm)**: `examples/dsl/{.note,notebooks,scripts,outputs,yaml}/` (incl. the
  unreferenced `native_2q_minimal.metal.yaml`) and `docs/codex_notes/` (all 22 `dsl_v3_*` notes →
  `docs/` now gone). `examples/dsl/` is now **geo-only** (`README.md` + `geo/`).
- **Untracked `refer/`**: `git rm -r --cached refer/` (files stay on disk) + added `refer/` to
  `.gitignore`; also dropped the now-stale `!examples/dsl/outputs/*.msh` negation.
- **Fixed dangling code refs**: `gmsh_adapter.py:216` error msg dropped the `.note/gmsh_walkthrough.md`
  pointer; `__init__.py` module docstring rewritten to foreground the native-geo path (was legacy-only).
- **READMEs rewritten** for the native-geo path: top-level `README.md` (2-layer model, binding key
  `role::layer::comp::prim`, the GDS+Palace fork, emit_geo cells, C-matrix→Hamiltonian, gds_viz,
  install/CLI with real output) + `examples/dsl/README.md` (geo/ examples + geo_build usage).
  Synced stale `AGENTS.md` (was "M1–M5") to current `CLAUDE.md`.

## Verification
- **Deps**: conda `metal-env` already complete — qiskit_metal 0.5.1, gmsh 4.11.1, gdstk 0.9.62,
  shapely 2.0.7, numpy 1.24.4; Palace 0.16 (`PALACE_BIN` set). `gdsfactory` absent (viz extra) → 2
  gated skips.
- **Pipeline end-to-end (跑通整个链路)**:
  - `chip_layout.meta.yaml` (M1 authored .geo) → GDS + MSH + Palace JSON + PNG (1414×1048,
    matplotlib backend), 9 physical groups.
  - `cells_2q.meta.yaml` (M5a emit_geo) → `cells_2q.elaborated.geo` + GDS + MSH + Palace JSON, 8 groups.
  - `two_pads.meta.yaml --run-palace` (LIVE Palace solve, 152 s) → `chip.results.yaml`: Maxwell
    C = `[[24.73,-1.98],[-1.98,24.73]]` fF, tier-2 Hamiltonian (L_J=10nH → E_C≈0.788 GHz,
    f01≈9.365 GHz, g≈374 MHz, E_J/E_C≈20.7).
- **Tests**: full suite **314 passed, 3 skipped** in `metal-env` (unchanged before/after the fixture
  relocations).
- **Adversarial review** (3-agent workflow `verify-v3-cleanup`): README accuracy = clean (0 issues);
  cleanup-state = all 7 checks pass; dangling-refs caught **3 blockers** = stale header comments in
  the 2 relocated fixtures (pointed at deleted demo runners + deleted `native_2q_minimal`) → **fixed**
  (relabeled as test fixtures, dropped the `运行:` pointers). Final re-scan of shipping tracked files
  = CLEAN.

## Follow-up — install gdsfactory (viz extra) + cross-platform requirements.txt
User: "gdsfactory 依赖没装, 装上并跑通, 然后加上跨平台 requirements.txt 提交."

**Dependency conflict (two pinch points), resolved:**
- qiskit-metal 0.5.1 pins an OLD stack (`numpy~=1.24`, `scipy~=1.10`, `pandas==1.5.3`,
  `shapely~=2.0`, `matplotlib~=3.7`). Latest gdsfactory 9.43 wants numpy≥1.26 / scipy≥1.17 →
  would break qiskit-metal. **Fix:** constrain `gdsfactory<9.3` → resolves to **9.2.2** + kfactory
  1.2.2 + klayout 0.29.12, keeping the numpy stack at 1.24.4 (installed with `-c` constraints file).
- kfactory 1.2.2 then **crashed at import** under the env's `pydantic 2.13.4`
  (`CrossSectionModel is not fully defined`). pyaedt (a qiskit-metal dep) caps `pydantic<2.12` and
  the installed 2.13.4 already violated it. Bisected: 2.11.10 still breaks, **2.10.6 works**. →
  pinned `pydantic>=2.6.4,<2.11`. (pyaedt not imported in the test path; downgrade is net-correct.)
- **qiskit-metal 0.5.x is conda-forge-only** (PyPI stops at 0.1.5) — so `requirements.txt` is a pip
  LAYER on a conda env, documented as such.

**Verified:**
- gdsfactory 9.2.2 imports; `gf.import_gds` works. Gated tests `test_to_gdsfactory_component` +
  `test_preview_gdsfactory_backend` now **PASS**; `geo_build --png` reports `backend=gdsfactory`.
- Full suite **314 passed, 3 skipped** (no regression from pydantic 2.10.6). Skip set rotated: the
  2 gdsfactory-present tests now pass, the 2 "gdsfactory-absent fallback" tests now skip (mutually
  exclusive). Pinned scientific stack confirmed unchanged (numpy 1.24.4 / scipy 1.10.1 / pandas 1.5.3).
- `requirements.txt` fresh-resolution sanity-checked; `pyproject.toml` `viz` extra capped to
  `gdsfactory<9.3` + `pydantic>=2.6.4,<2.11`.

**New/changed files:** `requirements.txt` (new), `pyproject.toml` (viz extra bounds), `README.md`
(install + test-count notes).

## Notes / open
- **Pre-existing uncommitted edit** (not authored this session): `.claude/status.md` had the stale
  "PR #14 open" bullet removed (correct — PR #14 is merged). Folded into this session's status update.
- Env side effect (not in git): `pydantic 2.13.4 → 2.10.6` in conda `metal-env` (required for
  gdsfactory/kfactory; also re-satisfies pyaedt's `<2.12`). gdsfactory 9.2.2 + deps added to the env.
- **Committed** on branch `chore/viz-deps-and-cleanup` (the v3 cleanup + READMEs + gdsfactory/
  requirements work) — see Resolution.
