# Session 2606081710 — xhigh re-review of #14: fix critical, issue the rest

**Branch**: `fix/review-critical-robustness` (off `main` @ `2f7d3ab`).

## Goal
Ran an xhigh `/code-review` of the merged native-geo PR #14 (9 finder angles + sweep,
adversarial verify). Then, per user: **fix only the most critical issues, file issues for the rest.**

## Locked decisions
- "Most critical" = silently-wrong-result correctness bugs with **surgical, low-risk** fixes that
  do **not** change any validated happy-path output (two_pads C-matrix etc.). Three qualified.
- Anything that **changes validated physics numbers** (e.g. carved-ground mesh refinement) or needs
  design discussion → **issue**, not a rushed edit.
- The conductors-as-voids carve (Approach A) is **correct by design**, not a bug — only its
  split-vacuum *fallback* was wrong (user asked "是挖错了吗" — no).

## Work done — 3 fixes (each + regression test)
1. **`_gmsh_layers.carve_conductors`** (`:313`) — warn-and-continue → **raise `DesignDslError`** when
   the carve splits the vacuum into ≠1 connected volume (was silently keeping `vac_tags[0]` →
   incomplete Palace dielectric domain → plausible-but-wrong C-matrix). Added `from .errors import
   DesignDslError` (+ updated the file's allow-import note). Test: `test_geo_pipeline.py::
   test_carve_conductors_raises_when_vacuum_splits` (synthetic full-cross-section slab bisects a box).
2. **`circuit_model._invert_matrix`** (`:197`) — absolute `|pivot| < 1e-300` floor → **scale-relative**
   `|pivot| ≤ 1e-12·max|entry|` (pure ratio test, scale-invariant under the fF→F ×1e-15). Near-singular
   / ill-conditioned (cond ≳ 1e12) C-submatrices now raise instead of inverting to garbage E_C/f01.
   Test: `test_circuit_model.py::test_near_singular_capacitance_matrix_rejected`.
3. **`palace_adapter._parse_palace_cap_csv`** (`:520`) — added `math.isfinite` guard so a diverged/
   partial Palace solve emitting `nan`/`inf` raises instead of propagating NaN into the Hamiltonian /
   `chip.results.yaml`. Added `import math`. Test: `test_palace_config.py::
   test_parse_rejects_non_finite_values` (parametrized nan/inf/-inf).

## Verification
- 3 affected files: **83 passed, 1 skipped** (metal-env).
- **Full suite: 319 passed, 3 skipped** (baseline was 314/3; +5 = the new tests; 0 regressions).
- 5-agent workflow adversarially verified all 3 patches → **all "sound"** (no false-reject; legit
  single-volume carve path byte-identical; no IndexError; no caller depends on the old warn/None;
  CSV guard rejects no valid output). Same workflow drafted the two issue bodies.

## Issues filed (the rest)
- **#18** — carved metal-ground faces omitted from mesh refinement (`_gmsh_mesh._conductor_surface_tags`
  misses `tracker.ground_faces`) → biased C on the full-chip carved path. MEDIUM-HIGH; deferred because
  the fix **changes validated C-matrix numbers** and needs a live-Palace re-validation.
- **#19** — consolidated robustness/silent-failure hardening checklist: `_conductor_groups` prefix
  collision; WSL `repr()` quoting; symmetry accepted-but-broken on geo path; substrate-outside-vacuum;
  carved-ground z-nudge over-application; Maxwell non-symmetrization; `_assemble_loops` open-loop;
  `geo_emit` unsanitized `::` names; `by_role`/`solver.device`/geo-ports silent no-ops; cells+geo
  precedence; cleanup bundle (SI_PER_INTERNAL/`_sanitize`/H_PLANCK dup, `_polygon_area` dead,
  `geo_names` dead param).
- Commented on **#15** — its singular-guard checkbox is addressed by fix #2 above.

## Open / next
- Push `fix/review-critical-robustness` + open a PR (not yet done — awaiting go-ahead).
- Another agent is fixing `examples/chip_layout.geo` (etch ground pockets so pads become isolated
  islands — electrically correct). Disjoint from this branch (src only). After they commit, merge.
  Caveat passed on: keep the moat from pinching the vacuum against the airbox wall, else fix #1's new
  raise fires (correctly).
