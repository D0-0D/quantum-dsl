# Session 2606272333 — qm4q repro: 核实 spec 阻塞点 + 多真空体 carve 修复

## 目标
用户指向 `.claude/qm4q-repro-spec.md`(AI 写的 spec:复现 qiskit-metal 教程 1.3
单个 TransmonPocket → 电容矩阵,与 Elmer 对比)。要求**完全核实 spec 声称的阻塞
点是否外部依赖导致**,然后给完善方案并推进。手机远控 / 每步先确认风格。

## 核实结论(spec 不准确)
**阻塞点是内部的,不是外部依赖。** 亲手逐项验证:
- **外部依赖全 OK**:Palace 0.16($PALACE_BIN)、Elmer 26.2、qiskit_metal 0.5.1
  (TransmonPocket + renderer_elmer/gmsh)、gdstk 0.9.62、shapely 2.0.7、clone 在
  5eb380fd。Elmer 7×7 参考 + Palace v1 6×6 输出都在、数值与 spec 一致。
- **唯一外部出入(无害)**:gmsh 实际是 **4.11.1**,非 spec/CLAUDE.md 写的 4.15.2
  (4.15.2 是 clone 的 `[mesh]` extra pin 的版本)。
- **spec 把真阻塞讲错了**(3 处):
  1. 说 .geo 是 "BooleanUnion 坏版、extrude 失败"。实际 build 跑通(exit 0),无失败。
  2. 真症状是**静默**:6 个导体只注册了 5 个 — `conn_a` 被吞(tag 碰撞:第一个
     BooleanUnion 结果被 ground 后续 Boolean churn 冲掉)。
  3. 号称 `_exp_d.geo` 里有"已验证修复" — **该文件根本不存在**(scratchpad 只剩
     exp_a/b/c)。无据。

## 锁定决策
- **本轮范围**:电容矩阵对比(我们 Palace vs Elmer)。
- **参考侧 env**:从 clone 另建独立 conda env(权威 quantum-metal 0.7.3 +
  gmsh≥4.15),metal-env 不动。(用户拍板 — 版本错位正是"conda 0.5.1 二进制 vs
  源码构建"。)
- **carve 真空切分**:方案 A — 支持多真空体(用户在 几何 hack / 先 scope 中选 A)。

## 完成的工作
1. **qmetal-src env 建好**(权威参考侧):`conda create -n qmetal-src python=3.11`
   + `pip install "/home/administrator/qiskit-metal[mesh]"`(清华镜像 — 默认 PyPI
   仅 ~40-150 kB/s)。结果:quantum-metal **0.7.3**、gmsh **4.15.2**、gdstk 1.0.0、
   shapely 2.1.2、scqubits 4.3.1、qutip 5.3.0。**坑**:PyPI gmsh wheel 缺
   `libGLU.so.1` → `conda install -n qmetal-src -c conda-forge libglu`(libgmsh
   RUNPATH `$ORIGIN/../lib` 即可找到,无需 LD_LIBRARY_PATH)。验证:TransmonPocket
   + renderer_elmer/gmsh + LOM Cell/Subsystem/CompositeSystem 全部可导入。
2. **qm4q 几何 tag 修复**(提交 37ff0fe):重写
   `examples/dsl/geo/qm4q_transmon_cell.geo` — ground 先建、金属用显式高位 tag
   (≥1001)、pad+lead BooleanUnion 成单面。6 个导体物理组现全部注册(原来 5 个、
   conn_a 被吞)。在 load_geo 层级验证。
3. **多真空体 carve 支持**(提交 38092df):`_gmsh_geometry`(vacuum_extra 字段 +
   remap)、`_gmsh_layers`(carve 保留所有切出的真空体、不再因 >1 报错;fragment +
   resolve 跨 box+extra)、`_gmsh_physical`('vacuum' 材料跨所有体)、
   `test_geo_pipeline`(切分→全保留 + 0 体→报错 两条测试)。

## 验证结果
- `pytest -k carve`:**3 passed**(src 改动后)。
- 全 geo 套件 `test_geo_pipeline + test_geo_binding + test_geo_emit`
  (QDSL_RUN_PALACE=1):**44 passed, 1 FAILED** — `test_two_pads_live_capacitance_matrix`。
- two_pads 失败已根因:多真空体改动的**第一版**把不连通的"衬底下方"真空块也留了
  (fragment 把 two_pads 真空切成 上/下衬底 两块)→ Palace/MFEM 读网格时在
  `AddTriangleFaceElement / GenerateFaces` abort(signal 6)。用 git stash 隔离确认
  (无我的改动时 two_pads 正常求解 >2min;有改动时 ~10s abort)。
- **已修**(在 38092df 的 remap 里):bulk vacuum_box 只取主 fragment 块(原行为);
  只有 carve 出的薄片进 vacuum_extra。mesh-only 检查:坏版 'vacuum' 组含 2 体,修后
  恢复 1 体。

## 待办 / 阻塞(从这里继续)
1. **two_pads 实解回归尚未在 remap 修复后重验**。重跑:
   `QDSL_RUN_PALACE=1 pytest tests/test_geo_pipeline.py::test_two_pads_live_capacitance_matrix`
   — 必须仍为 `[[24.73,-1.98],[-1.98,24.72]]` fF。(实解慢,>2 分钟。)
2. **qm4q v2 仍在 gmsh mesh.generate 失败**(在 Palace 之前):`Invalid boundary
   mesh (overlapping facets) on surface 1088 1089`。多真空体修复让管线越过了 carve,
   但薄层(2µm 金属 + ε-nudge 的 1µm 真空)+ carve 薄片在 min_size=8µm 下产生重叠
   facet。**未解决**。carved-ground 衬底 ε-nudge(CARVED_GROUND_SUBSTRATE_GAP_SI)
   已生效(非此因)。
3. qm4q 的 carve 薄片 fragment 后是否与主真空连通(MFEM 合法性)— 未测(被 #2 挡住)。

## 下一步(建议)
- 重验 two_pads(#1)确认无回归。
- **转到参考侧**:在 qmetal-src 跑 qiskit-metal QGmshRenderer + Elmer(教程 4.19
  模式)对同一 TransmonPocket → 权威 7×7 电容矩阵。这是**必须的产物**,且能看到
  qiskit-metal 怎样给同样几何干净划网格(很可能指导 #2 的修法 — 例如 2D 薄片金属
  vs 我们的 2µm 厚 3D carve,或其网格设置)。
- 再带着这个洞见解决我们这侧网格(#2),完成对比。

## Git
- 分支 `fix/carve-multi-vacuum`(从 main 切出)。
- `38092df` — 多真空体 carve 支持(src + test)。
- `37ff0fe` — qm4q 几何 tag 修复。
- 未提交 / 未碰:`qm4q_transmon_cell.meta.yaml`(上一会话)、
  `docs/report/{01,02,note,todo}`(上一会话)、`.claude/qm4q-repro-spec.md`
  (spec — 未碰)、`examples/qiskit_metal_ref/`(用户的 notebook)。
