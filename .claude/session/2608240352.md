# Session 2608240352 — v4 立项: 空白起点 + 契约 + 可执行需求

## 目标
用户指示: **完全空白的开始** —— orphan 分支空树, 新计划/契约文档, 以及一套**简洁的
py3.13 测试, 最小地给出所有待实现需求**。(推翻同日早先的「删减式重构」方案 ——
那个 worktree/分支已删。)

## 锁定的决策
- **greenfield**: 分支 `v4`(orphan, 无历史), worktree `~/quantum_dsl-v4`。旧实现只作参考,
  算法逐文件审后搬运;qiskit_metal / `.metal.yaml` / v3 模板引擎不进 v4。
- **从 v3 继承三样**: `.geo` 四段 Physical 名约定(fixtures 直接复用)· 内部单位 µm ·
  物理 golden(与实现无关)。其余(meta 词汇、Python API)全部重新设计——
  meta 扁平化(`simulation.gmsh.*` 嵌套废弃), API 顶层扁平(`load_geo`/`load_meta`/
  `build_gds`/`build_mesh`/`palace_config`/`parse_capacitance`/`solve_circuit_model`/
  `assemble`/`lumped_cpw`/`build`…), 绑定键 = component 段。
- **测试即契约**: `tests/test_spec.py` N0–N14, 每测试函数内 import(逐条红, collection 不炸);
  live 两条 gate `QDSL_RUN_PALACE=1`。
- golden 锚点: two_pads 实测 C `[[24.7288,-1.976],[-1.976,24.7293]]` fF + LOM 闭式值
  (C_Σ 24.5714 / E_C 0.7883 GHz / f01 9.3649 GHz / g 375.01 MHz)、CPW 参考
  (typical_10_6_si)、χ golden(dispersive_shift −117856.239 Hz)、λ/4 LC、
  SQUID 甜点解析值(12+8 GHz, φ=0.5 → 4 GHz)、Schur 3×3 手算、Palace verbatim CSV。

## 做了什么
1. 删除早先的 `~/quantum_dsl-retire` worktree + `refactor/retire-qiskit-metal` 分支(方案作废)。
2. `git worktree add --orphan -b v4 ../quantum_dsl-v4`(真空树)。
3. 写 `SPEC.md`(目标/边界、继承三样、Layer-1 新词汇、N0–N14 API 表、验收)。
4. 写 `tests/test_spec.py`(N0 平台纯度 / N1 单位 / N2 load_geo / N3 load_meta /
   N4 GDS / N5 mesh / N6 Palace config+CSV / N7 live×2 / N8 电路模型含 SQUID+nan 守卫 /
   N9 拼装累加+Schur+错名 raise / N10 CPW 含动力学电感主导 / N11 TL+χ / N12 圆角 cell /
   N13 build+manifest sha256 / N14 分块落盘)。
5. fixtures: `two_pads.geo`(从 v3 原样拷)· `two_pads.meta.yaml` + `two_pads_blocks.meta.yaml`
   (新词汇)· `palace_postpro/terminal-C{,m}.csv`(Palace 0.16 verbatim)。
6. `pyproject.toml`(py≥3.13, deps 仅 pyyaml/shapely/numpy, gmsh/gds 为 extra)、
   `CLAUDE.md`、`README.md`、journaling 初始化。
7. 建 conda env **`qdsl313`**(python 3.13 + pytest/pyyaml/shapely/numpy/gmsh/gdstk;
   `HWLOC_COMPONENTS=-gl` 已持久化)。

## 验证
- qdsl313 实测: Python **3.13.14**, gdstk 1.0.1, shapely 2.1.2, numpy 2.5.2, gmsh import OK
  (踩到 libGLU 坑 → `conda install -c conda-forge libglu` 解, 与旧仓 2606272333 同款);
  `HWLOC_COMPONENTS=-gl` + `PALACE_BIN` 已持久化。
- `pytest tests/ -q`(qdsl313, 空树): **38 failed(全部 ModuleNotFoundError = 待实现需求),
  1 passed(N0 py3.13 平台自检), 2 skipped(N7 live gated)**, collection 干净, 0.27 s。✅
  这就是 TDD 起点态: 红名单 = 需求清单。

## 下一步
- 按 plan.md V4-1…V4-6 实现(V4-2 几何与 V4-3 物理内核可并行 worktree agent)。
