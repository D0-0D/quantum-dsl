# 2026-08-24 · V4-3 de-risk: 原仓物理问题调研 + gmsh→Palace 管线本机实证

## 目标(用户指令)

原仓(D0-0D/quantum-dsl)物理仿真历史问题很多(issue + 论文/Elmer 对照例子
的误差), 先派 agent 摸清问题, 再**确保 V4-3 所依赖的 gmsh+Palace 物理管线在
本机能正确实现**——做到这一步为止; V4-1/V4-3 的实现随后由用户编排 subagent。

## 做了什么

1. **调研**(general-purpose agent, 全量读 issues #1–#28 + 旧仓 session 取证):
   16 类问题 + 15 条规则 + 5 个对照案例, 蒸馏进
   [`../physics-pitfalls.md`](../physics-pitfalls.md)(实现者行动版)。
   最大的坑: ε-nudge 压低 C 29%/耦合 40%; fragment 静默损坏拓扑(整张衬底
   界面被误接地); 双岛当单岛 C_Σ 错 1.70×; 单网格数字自带 6–21% 不确定度;
   gmsh 4.15.2(qdsl313 正装着)在 qiskit-metal 路径上实证过 OCC 退化。
2. **管线本机实证**: 读 v3 源码提取 void-carve 配方, 写 ~250 行独立原型
   [`../proto/two_pads_pipeline.py`](../proto/two_pads_pipeline.py)
   (qdsl313, 无 qiskit_metal), 实跑 gmsh 4.15.2 → Palace 0.16:

   | 变体 | 做法 | 对 N7 golden 最大相对误差 |
   |---|---|---|
   | A | v3 复刻: dilate µm→m, L0=1.0, order2, mesh 40/4 | **0.038%** |
   | B | v4 候选: 全程 µm, 不 dilate, **L0=1e-6** | **0.072%** |
   | A mesh 20/2 | 网格加密(33k→244k 节点)收敛探针 | −0.7~−1.18%(PASS) |
   | A order 1 | 同网格降阶敏感性 | +4.2%/+7.3%(**FAIL**) |

   ⚠ 金属厚度 2 µm 是 golden 的一部分(v4 meta 无此键 → 实现里固定);
   golden 未完全收敛(真值低 ≈1%), **order 2 是硬承重件**。
3. **V4-3 数学链条复算**: 逆电容 LOM(E_C/C_Σ/f01/α/EJ_EC)、
   g=½β√(f01·f01)、SQUID hypot 形式、单岛闭式——全部对 N8/N7 契约 golden
   逐位(<1e-9)吻合, 公式口径已钉死写入 pitfalls §0。

## 结论

- **V4-3 可以正确实现**: 物理管线(V4-2/V4-6 依赖)与数学内核(V4-3 本体)
  双双在本机对 golden 实证通过; 配方逐字记录在 pitfalls §0。
- **v4 建议采用变体 B**(µm 网格 + L0=1e-6): 精度与 A 等同, 免 dilate
  重建 → 原仓 A2 类坑(fragment 尺度条件化)整个不存在。
- 契约 N7 只覆盖无 ground 的 two_pads; ε-nudge/面归位/尺寸场三条规则的触发
  路径要等将来带 ground 的 fixture(pitfalls §3 记了债)。

## 下一步

用户编排 V4-1(纯 python 基础)与 V4-3(纯 math 内核)subagent 实现;
之后 V4-2 照 pitfalls §0/§1 配方写 build_mesh/palace_config。
