# 2026-08-24 · 物理管线 de-risk + 定架构(gmsh→Palace→C→LOM)

## 目标(用户指令)

原仓(D0-0D/quantum-dsl)物理仿真历史问题很多, 先派 agent 摸清问题, 再确保
V4-3 依赖的 gmsh+Palace 物理管线能正确实现——**整体重新审视这一步, 不是
一比一复刻 v3**。V4-1/V4-3 的实现随后由用户编排 subagent。

## 过程

1. **调研**(agent 全量读 issues #1–#28 + 旧仓 session 取证)→ 原始报告存
   [`../v3-issue-survey.md`](../v3-issue-survey.md)。
2. **v3 配方复刻实证**: 独立原型([`../proto/two_pads_pipeline.py`](../proto/two_pads_pipeline.py))
   实跑 gmsh 4.15.2 → Palace 0.16, 复现 N7 golden 至 0.038%; 顺带做了
   µm+L0=1e-6(0.072%)、网格加密(−1.2%)、order 1(+7.3%, FAIL)三个敏感性点。
3. **用户批评第一版文档**(physics-pitfalls.md): 上下文指代混乱、只堆碎片、
   15 条规则像在复刻屎山、没有整体管线图像与决策论证。成立。
4. **重新审视架构**: 追问"金属为什么非要挖空?" → 写零厚度片原型
   ([`../proto/two_pads_sheet.py`](../proto/two_pads_sheet.py)): 焊盘面直接
   fragment 进衬底/真空界面, Terminal 挂内部边界面。
   - 首跑 abort: `"A non-periodic face cannot have multiple boundary elements"`
     —— 根因是 .geo 作者 Physical 组在 imprint 路径下存活, 同面双边界元;
     `removePhysicalGroups()` 后**直接可解**。
   - **推翻 v3 的架构前提**: "Palace 拒绝内部面 Terminal" 系误诊(v3 大概率
     踩的就是同款双边界元 abort)。v3 的挖空/ε-nudge/scale-ladder/腔壁归位
     整条机器由此在 v4 失去存在理由。
   - 零厚度片 C 对 golden 偏 1.46%(PASS), 且与挖空方案细网格值只差 ~0.1%
     ——差值主要是 golden 自己的网格偏置 + 2 µm 板厚效应。
5. **文档重写**: [`../physics-pipeline.md`](../physics-pipeline.md) ——
   从"我们在做什么"与物理图像(导体=边界条件)出发, 管线全景图、金属表示
   三方案对比与裁决、ε-nudge 来龙去脉、单位/L0/order 语义、收敛实测表、
   N7 契约张力(golden 是 v3 配方锚点, 建议用零厚度片实测重钉, 待用户拍板)、
   通用失效防线。旧 pitfalls 文档删除。
6. **外部取证**(codex gpt-5.5 + web_search, 后台): Palace 官方示例的金属
   表示 / 业界零厚度 vs 膜厚惯例 / L0 与输出约定官方定义。
   job: `~/.codex-jobs/20260824-070412-palace-metal-modeling/`。结论回填
   pipeline §10。

## two_pads 实测汇总(golden ±2%)

| 表示 | 网格/阶 | 最大偏差 |
|---|---|---|
| 挖空 2µm 板, dilate+L0=1(v3 复刻) | 40/4, o2 | 0.038% ✓ |
| 挖空, µm+L0=1e-6 | 40/4, o2 | 0.072% ✓ |
| 挖空, 加密 | 20/2, o2 | 1.18% ✓ |
| 挖空, 降阶 | 40/4, **o1** | **7.25% ✗** |
| **零厚度片(v4 路线)** | 40/4, o2 | 1.46% ✓ |
| 零厚度片, 加密 | 20/2, o2 | 2.87% ✗(golden 的板厚+网格工件偏置所致, 见 pipeline §6/§7) |

V4-3 数学(N8 LOM/SQUID、N10 AGM、N11 χ/LC)对契约 golden 逐位复算通过;
v3 `cpw_analytic.py` 在 py3.13 命中 1e-16, 可审后原样搬。

## 结论与下一步

- v4 金属表示 = **零厚度片 imprint**(pipeline §4); 挖空原型保留作对照工具。
  codex 文献取证(158 检索, [`../palace-lit-survey.md`](../palace-lit-survey.md))
  独立佐证: Palace 官方例/KQCircuits/SQDMetal/Q3D 全是零厚度 PEC 惯例;
  1 µm 板厚 ≈7.6% 几何偏差(真实膜 100–200 nm ≈1–1.8%)。
- **N7 golden 已重钉**(用户批准 "spec&test 只是初步框架, 可按需改"):
  C=[[24.5324,−1.9472],[−1.9472,24.5353]] fF, f01=[9.3989,9.3984] GHz,
  配方=零厚度片+40/4+order2+接地盒。旧 v3 值仅存 git 历史与 pipeline §6 表。
- 用户编排 V4-1/V4-3 subagent; V4-2 照 pipeline §3–§6 实现。

## 后续同日: 分块拼装取证 + 并行调研 + 真机实验(会话分支"预研第二步")

- 用户质询 SPEC "绑定键/跨块互容必丢"是否合理 → codex(gpt-5.6-sol, 86 检索)
  取证([`../assemble-lit-survey.md`](../assemble-lit-survey.md)): 与
  qiskit-metal LOM 2.0 机制同构(源码行级), 作为披露的 quasi-lumped 近似
  **合理**; 但"直接腿天然可忽略"不成立(公开设计 <1%~15%, 相消 coupler
  零点敏感)。SPEC 措辞已收紧(共现才有直接互容; 共享 ground 不算路径),
  pipeline 新增 §11。
- 并行再派两路 codex: `lom-formula-conventions`(g/χ/SQUID/浮动双岛/CPW
  公式口径 vs 文献)、`palace-ops-practicalities`(order2 多 rank 上游状态/
  AMR/升级路径/域尺寸惯例)。
- 用户提供 64C/128G 真机(ssh 47.121.146.127:55776) → spack 构建 Palace
  0.16(spack 最新 safe 版恰同本机), 用 two_pads 20/2 细网格(~1.7M 未知量,
  v3 issue #22 问题规模)实测 order2 多 rank——判别 WSL 环境 vs Palace bug。
- **真机结果(接管会话跑完)**: 构建历经 5 轮(缺 -dev 包 ×2、zlib 多架构
  目录、petsc 镜像包 sha 损坏→官方源种缓存)后成功; np=4 与 np=32 均
  rc=0, C=[[24.3424,−1.9192],[−1.9192,24.3465]] fF, 两档 rank 一致到
  1e-12 且与 WSL C′ 实测逐位吻合。**issue #22 判为 v3 环境/构建问题,
  非 Palace bug**(pipeline §6 / v3-issue-survey A6 已更新); 裸机不加
  HWLOC_COMPONENTS=-gl 亦正常, 该 hang 确系 WSL 特有。
- **两路 5.6-sol xhigh 调研收割**(与 5.5 独立跑交叉验证, 结论一致):
  - `palace-ops`(152+72 检索, [`../palace-ops-survey.md`](../palace-ops-survey.md)
    → pipeline §12): 上游无 order2 多 rank 静电 bug(与真机互洽);
    estimator 每 terminal 必跑; AMR 不替代边缘 seed; 0.17 无静电行为
    变化, 留 0.16 无代价; 外边界不写即 ZeroCharge; 域尺寸无定律用域扫。
  - `lom-formula`(202+68 检索, [`../lom-conventions-survey.md`](../lom-conventions-survey.md)
    → pipeline §2 重写): E_C/C_Σ/E_J/f01/SQUID/浮动双岛/CPW 全部确认
    标准; **g 是文献真实分叉**(Blais/推导=裸 f_p vs Krantz 书写/契约=
    修正 f01, EJ/EC=20 时差 7.9%)——契约钉 f01 支, 跨文献比对先对口径;
    **χ 引文修正**: 两式实为非 RWA 三能级二阶(Zhu 1210.1605), Koch
    (3.9)/(3.10) 是 RWA 版, qiskit 源码注释误引(契约 N11 注释已改);
    对称化前先查 ‖C−Cᵀ‖/‖C‖(§8 防线补充); CPW 搬运审点 = λ_g 必须由
    总 L'C' 重算、有限衬底 sinh 分支。
- **N15 外部物理验证进契约**(用户质询"golden 不该是论文值吗"引出):
  澄清 N7 从来是自参照回归锚(two_pads 无论文值可锚), 缺的是外部物理锚
  → sung(PRX 11.021058)fixture 从 v3 移植(geo component 段改岛级
  QB1_t/QB1_b——v4 绑定键=岛, v3 的整段 component 会把双岛短路), meta
  重写为 v4 词汇(islands 列表/E_J/outer_boundary: open); 断言 C_Σ ×3
  对论文 ±5% + β_qc ±20%, 结构性排除 C_12/绝对 g(账见 fixture 头注)。
  可行性已核: v3 的"分块 o2 −3%"确系 +12~15% 与 −20~22% 抵消(session
  2607290329 原话), 但 Elmer 对论文 1.00–1.03 是独立交叉; 且当年三大误差
  源(分块虚高/o1 欠收敛/#22 挡整片 o2)在 v4 全部结构性消除——N15 将是
  整片 o2 的首次干净测量。配套: N8 新增浮动双岛闭式 case(C_eff=52.2̄ fF
  手算 golden)+ coupling β 进 API; SPEC 词汇扩展(islands/E_J/
  outer_boundary), 验收加第 3 条。
