# 2026-08-24 12:51 — V4-1 基础 + V4-3 物理内核 (N0–N3, N8–N11 全绿)

## 做了什么

双线并行 (主会话 V4-1, fork subagent V4-3, 文件所有权切分无交集):

### V4-1 基础 (N0–N3, 14 条)
- `src/quantum_dsl/{errors,units,geo,meta,__init__}.py`。
- `parse_length` → µm (裸数=µm, bool 先挡); `parse_quantity` → SI,
  裸数无量纲一律拒 (后缀 = SI 词头 + Hz/Ohm/H/F, 最长单位先匹配)。
- `load_geo` 用 **gmsh 惰性 import** 解析 (fixtures 有变量/宏/Include,
  文本解析撑不住 bbox; N0 只禁 import 时拉起)。四段 Physical 名
  `role::layer::component::primitive` 校验, bbox 直接取 gmsh 模型包围盒。
- `load_meta`: schema `quantum-dsl/meta/1`, 未知顶层键 raise;
  `circuit_model.qubits` 的 `L_J`→亨利, `E_J`/`squid.E_J1/2` 论文惯例是
  频率 (如 `12.2GHz`) → 解析成 **Hz (E_J/h)**, ×h 换焦耳留给 build 接线。

### V4-3 物理内核 (N8–N11, 16 条; fork 实现, 主会话审后合入)
- `circuit_model.py`: v3 算法审后搬运 (B 变换/完整求逆取 θθ 块/hypot
  SQUID/scale-relative 奇异判据/isfinite 拒 nan-inf-0-负)。适配: 新签名
  `solve_circuit_model(labels, maxwell_fF, junctions=dict列表)`;
  CouplingResult 暴露 `beta` (纯几何) + `g_MHz` (g 钉 f01 支)。
  **新增防线** (pipeline §2/§8): 求逆前查 `‖C−Cᵀ‖_F/‖C‖_F > 1e-6` raise;
  未被 junction 认领的 label 禁静默接地 → raise。
  **χ 引文修正**: Zhu et al. arXiv:1210.1605 Eq.(11)–(14) 非 RWA 三能级
  (勿引 Koch (3.9)/(3.10) RWA 版); χ = 单边 pull, 非 2χ。
  YAGNI 未搬: C_j、C_sigma_geometric、resonator 进 solve 机制 (V4-5 需要再搬)。
- `assemble.py`: 契约版显式 keep 列表 (首现定序累加 + keep 外全 Schur +
  keep 拼错 raise); v3 的 junction 推断/ground_node/audit 未搬。
- `cpw.py`: v3 近乎原样 (字面常数、AGM、「已知不自洽」三条保留)。
  **审点结论**: golden 钉的是参考实现值 (Z0/λ_g **不含** Lk), 总 L'C' 重算
  属消费端 —— V4-5 由几何反推 f_res 时再做, docstring 已写明。

## 契约测试 harness 修复 (非 golden 改动)

`TestN9Assemble` 两条用了 `pytest.approx(嵌套list)` — pytest **从不支持**
(构造期 TypeError, 立项时全红是 ModuleNotFoundError 掩盖了它, assert 行
从未执行)。修为逐行 approx, **golden 数值逐字节不动**。实测 fork 的 Schur
输出 `[[9.25,-2.25],[-2.25,7.9167]]` 与测试内手算式逐位一致。

## 结果

`pytest tests/ -q`: **30 passed, 4 skipped (N7/N15 live 门), 10 failed**
—— 失败全是未开工里程碑 (N4–N6 → V4-2, N12 → V4-4, N13/N14 → V4-5)。

## 下一步

- V4-2 几何分叉 (build_gds/build_mesh/palace_config/parse_capacitance),
  先读 pipeline §4–§6。
- V4-4 cells、V4-5 编排、V4-6 live 收尾。
