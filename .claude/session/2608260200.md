# 2026-08-26 · v4.0 发布收口: v4 → main, tag v4.0, v4-dev 存档

**目标**: 把 orphan 分支 v4 转正为 `main`、打 `v4.0`, 旧 main 让位为 `v3`, v4 的 TDD
开发痕迹留 `v4-dev`; main 的文档/测试重组成对后续开发友好的正常形态, 自成体系。

## 做了什么(三个 commit)

1. **树重组**: `tests/test_spec.py`(40 条契约测试)→ conftest + 4 个按模块命名的文件
   (28 + 2 live); 只合并重复断言, golden 与翻案账逐字保留; N7/N15 各两条合成一条
   (一次求解同时断 C 与哈密顿量, N15 少跑一次 72 min)。`tests/fixtures/` 的例子提升为
   `examples/`(测试仍以它们为输入); `.claude/proto/palace_remote.sh` → `tools/`;
   六份调查文档、session logs、n15-evidence、原型脚本只留 v4-dev。
   顺手修一个真 bug: `build._write_block_geo` 把 `//` 注释里的示例 Physical 名当语句解析。
2. **docs/ 全部新写**: README(导航)/ architecture / physics(吸收 physics-pipeline 全部干货 +
   文献列表)/ grammar; report/ 01 展示 · 02 贴代码逐段讲 + 预期问答 · 03 演示手册 + 故障预案。
3. **发布准备**: pyproject 4.0.0、删未用的 numpy 依赖、LICENSE、README/SPEC/CLAUDE 重写,
   src docstring 的 `.claude/…` 引用全改指 `docs/physics.md §N`(行数不变, 文档 文件:行号 有效)。

## 实测

- `pytest tests/ -q`: 28 passed / 2 skipped, ~12 s。
- N7 live: 123 s, C 矩阵与 golden **逐位相同**(24.53243335381 / −1.947218428813 / 24.53533214664)。
- import 纯度(子进程)、文档相对链接(0 broken)、`.claude/` 残留引用(0)全过。

## 分支/tag

`main`(3 commits on top of v4 tip 8336ee7)· `v3`(= 旧 main f04be9d)· `v4-dev`(= 8336ee7)· tag `v4.0`。
推远端顺序: v3, v4-dev 先; main force-with-lease; tag; 删 origin/v4。之后关 issue
#15 #18 #19 #20 #22 #24 #27 #28(结论表在 docs/report/01 §6), 留 #23 #25 #26。
