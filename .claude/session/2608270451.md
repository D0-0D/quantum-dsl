# 2608270451 — build() 随 GDS 出 PNG 预览

用户: "build里加一下gds和gds导出的png"。GDS 早已是 build() 产物, 缺的是 PNG。

- `tools/gds_png.py` 的渲染搬进 `src/quantum_dsl/gds.py::render_gds_png(gds, out, jj_layers, px_per_um=None, bbox=None)`
  (PIL 惰性 import; 默认长边 2000 px; field − metal 布尔取真缝, 带孔 ground 不靠 PIL 填孔)。
- `build()`: `gds.by_role` 非空时紧接 `build_gds` 产 `<stem>.gds.png`, jj 层取 by_role.jj.layer 画品红; 进 result["gds_png"] 与 manifest outputs。
- `tools/gds_png.py` → 薄壳 (换比例 / 裁剪, jj 固定 20)。pyproject `gds` extra 加 pillow。`__init__` 导出 render_gds_png。
- 测试: N13 `test_build_no_solve_artifacts_and_manifest` 多查 gds_png 存在、在 manifest、PNG 恰两色 (two_pads 无 jj)。28 passed / 2 skipped。
- 文档: SPEC N13 行、architecture(流程图/模块表/API 表/§6 产物树)、README、report 03(预期输出)、report 04(两处工具引用)。

不做: meta 里加 png 分辨率键 (无需求; 要就调 render_gds_png/工具壳)。
