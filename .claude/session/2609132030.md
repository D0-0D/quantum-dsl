# 2026-09-13 20:30 — 代码审查 (N16/N17 两个提交) 的修复

上游: `/code-review max` 对 `50dc842` (N16) + `f869f73` (N17) 的 15 条发现 + 自查 1 条。**全部已修**, 无代码改动遗留。
⚠ 注意: S68 那轮在**别的 worktree** 改的 4 处 (net/geo.name/置毒/etch 层) 不在本 worktree, 本轮从 `f869f73` 重做, 覆盖面更大。

## `src/quantum_dsl/layout.py` (9 处, 全部曾静默通过)

| 位置 | 症状 | 修法 |
|---|---|---|
| ports 循环 | 局部 `net` 遮蔽 :406 新提前的路由体 net → 连接型模板一旦声明 `ports:` 就把错的 net (或 None) 交给 `_connect`/`subsystems` | 改名 `pnet`; `spec.get("island", body)` 不再走真值判断 |
| `_load_template` | 模板缺 `.geo` 的报错路径本身 `geo.name` 于 None → 裸 AttributeError | `yml.with_suffix('.geo').name` |
| `_run_geo` / `_geo_step` | `port(i)` 简写只被 `_refs` 看成基名 `port`, 真正读回的 `port_x/y/a/w` **没被 NaN 置毒** → 条件赋值时读到上一实例坐标 | 新 `_port_refs()`: 先 `_port_spec` 展开再置毒 |
| `_connect_pose` | `mirror: y` 取反模板局部 +x = 路由轴本身, 几何画向 `to` 口反方向而 `to` 端爪照并 net | 连接型只许 `mirror: x`, 其余 raise |
| `_template_step` | 连接型静默吞掉 `at:`/`rot:` (而 `mirror:` 本轮起生效, 作者会以为三者一组) | 显式 raise |
| `_connect` | 外挂端口背后零个面时内层循环什么都不匹配 → 路由"连"到一块没画出来的金属, 所有校验都看不见 | 空集即 raise |
| `enabled()` | `if:` 指向未声明参数 = 静默"关闭" (违反 CLAUDE.md 拒绝静默) | 不在 params 即 raise; 报错带段名 |
| `_load_template` | `islands/external/etch/ports/junction` 的**子字典**从不查键 (`iff:` 静默丢守卫); `kind:` 不校验取值 | `_ENTRY_KEYS` 逐条 `_check_keys`; kind 只许 `connect` |
| `_template_step` | `if connect and body` 真值判断: 无 `islands:` 时伪造岛键 (结绑到没画过的 component), 且 `0` 这个合法岛键被判假 | 一律 `body in islands` 成员判断 |
| `_add_input` | `Include` 的宏库不进 `Layout.inputs` → 改 `cpw_macros.geo` 而 manifest 哈希逐字不变 (违反 N13) | 新 `_included()` 递归走 `Include`, 模板与手写步骤都走 `_add_input` |

## `examples/lib/` 的 `.geo`

- **`cpw_macros.geo`: 宏名 `CPW`/`CPW_ARC`/`CPW_MEANDER` → `LIB_*`, 入参 → `_lib_*`。** 两个问题一起解决:
  ① `examples/qlib.geo:87` 也有 `Macro CPW` —— 同进程内先编译 chen 再 `load_geo(sung_2021_xmon.geo)` 会 "Redefinition of function CPW"
  炸掉整个 session, 且 qlib 的 guard 在中止点之前已置位 → 重试变成无关的 "Unknown function JUNCTION" (实测, 现已修复并验证);
  ② 裸名入参 (`x1/y1/width/R/acx…`) 是进程级全局, 会活到后续手写 `geo:` 步骤, 漏设一个就读到上一实例的陈旧值而不报错。
- `bar_coupler.geo`: 加前置守卫 —— `pent_off` 是绝对 µm 而 `D` 由端口间距注入, pitch 一小就把五边形连同结端点推到条外,
  而 `_junction` 只查长度 > 0, 于是静默记下一个不碰任何条金属的结。

## 物理 / 文档

- **`docs/physics.md` §13 + `chen_2025_3x3.meta.yaml`**: 补上块的边界条件 —— 块只保留自己 6 个 component, 块外的面当孤儿删掉,
  **包括块内比特伸向块外耦合器的爪** (H01 块少 5 只, 每只 ~6944 µm²、距盘边 12 µm, 载片地只隔 5 µm)。所以块解的对地项系统性偏低,
  §13 原本"残差不在 d"的结论是建在这个缺爪的块上的, 现已注明。量化要么整片解要么块内保留邻居爪再 Schur 消掉, 都还没做。
- `build.py` 新 `_warn_overlapping_blocks`: 块集合重叠 = 同一导体在多块里整块重求解, `assemble()` 会无条件累加其自电容。
  (先试过放在 `assemble()` 里, 但**跨切口的共享节点是它的本意**, 合法测试也会中招 → 挪到 `extract.blocks` 校验处, 那里分得清。)
- `tests/test_chen_2025.py`: 半盘 golden 从矩形近似换成圆缺闭式 `r²·acos(h/r) − h√(r²−h²)` (原式偏 +0.32%, 吃掉 5e-3 容差的 64%,
  且窗口偏斜: dia 390/391 都过而 389 不过); 容差收到 1e-3。CLAUDE.md: golden 只锚闭式。
- `docs/grammar.md` / `SPEC.md` / `examples/README.md`: 连接型的 `at/rot/mirror` 规则、宏命名约定、单块要自己
  `for_block` (`build()` 会先给整片出网格) 写进文档。
- 测试计数七处过期 (33 → 实际) 全部同步; 现在 **36 passed / 2 skipped**。

## 回归

`tests/test_layout.py::test_layout_guards_close_silent_paths` 一条覆盖全部新守卫 (含 `port(i)` 置毒与 Include 进 manifest)。
⚠ 写测试时踩到: YAML 1.1 把 `on` 读成 `True`, 模板参数别叫 `on`。
端到端复验: chen GDS 仍 87 多边形 (66 metal + 21 jj), 块网格标签不变 —— `.geo` 改名是纯重命名, 几何逐字不变。

## 补修 (同一轮, 用户指出「看到了却因篇幅裁掉」是自相矛盾 —— 裁剪是报告列表的格式限制, 与要不要修无关)

- **`build(blocks=[...] | "all")`** (`build.py` + `palace.py`): 只做指定的块, **跳过整片网格/config** (整片 ~9M tets, 本机跑不动 —— 这正是
  `.claude/chen-evidence/solve_block.py` 手抄 `_run_palace` / `HWLOC_COMPONENTS=-gl` / `_junctions_from_meta` 的原因);
  `solve=True` 时逐块跑 Palace 并写 `block_<name>.results.yaml`。`palace_config(..., output=)` 让每块有自己的 `postpro_block_<name>`,
  否则同一 out 下多个 config 互相覆盖 postpro。新 `_results_doc()` = 整片与分块**唯一**的 results schema; `_in_scope()` 按块标签筛
  qubits / subsystems (替掉脚本里硬写的 `("Q01","H01","Q11")`)。回归 `test_build_blocks_only_skips_the_whole_chip`。
  那个 scratch 脚本现在可以扔了 —— 等值调用是 `build(m, out, solve=True, blocks=["H01"])`。
- **24 条「结构性零互容」告警不是误报**(我上一条转述用词错了): 实测全是 coupler↔coupler 对, 相距 155.6 µm < side_um 300,
  而 SI 的孤立 QCQ 口径本来就不含耦合器间直接互容 → 告警字面为真。改成在 meta 里说明缘由, 不消音。
- `docs/design/component-library.md` §3: 改成实际实现 —— 爪在同一个 `.geo` 里画 (没有嵌套子模板)、只有 E/N/W/S 四只、无 RO 爪、
  端口在颈外端 (不是弧中点)、纵向键是 `mirror: x` (不是 `rot 90 + mirror`, 连接型写 `rot:` 现在 raise)、层表是 `{ta, jj}`。
- `.claude/status.md`: PNG 在 gitignore 的 `build/` 下, 改成「本地跑一次即有, 仓库里没有」。
- **`layers: {metal: ta, jj: jj}` 重复 21 次: 决定保留。** 设计稿 §1.5 的规则是「同名兼容可省」, 而 `metal` ≠ `ta` —— 它能省只是因为
  实现多了一条「该 kind 唯一」的兜底。写明映射更贴规则; 要去重复得引入版图级 `layers:` 默认值, 不值当。
- 测试计数再同步 (→ **37 passed / 2 skipped**); SPEC N14、grammar §分块、architecture 产物树同步。
