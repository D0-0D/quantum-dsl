# 2609161007 — `docs/grammar.md` 全面更新

## 任务
用户: 「全面更新 docs/grammar.md」。只改文档, 不动代码。

## 做法
对照 2026-09-16 `main` 逐文件核: `layout.py`(全文) / `meta.py` / `geo.py` / `units.py` / `mesh.py` / `gds.py` / `build.py` / `palace.py` /
`_gmsh.py` / `circuit_model.py`(认领规则), 五个模板 yaml + `.geo`, 三个版图例子, `tests/test_layout.py`(嵌套 / 守卫的真实用法),
SPEC N16/N17, 最近两篇 session。实际编译 chen / xmon_readout 版图取真实 Physical 名、端口键、qubits / subsystems 记账形态写进文档。

## 文档与代码的出入 (已修)
- §2.5 宏库: qlib 少列 `POLY` / `XMON`; `lib/cpw_macros.geo` 的 `LIB_` / `_lib_` 前缀与"宏是进程级全局"的原因没写; `Call X;` 独占一行没写。
- §2.1 / §3.2: `drawing` 层任何 role 都收、只进 GDS; `layers.<id>.gds` 也接受 `{layer, datatype}`; 没给 gds 的层不进 GDS。
- §3.2 `subsystems`: 原写"接受但不消费", 实际已**原样透传**进 `results.yaml`, 且版图定长路由往里追加条目。
- §3.3: 版图路线分块不派生 `block_<name>.geo`(模型内 `for_block` 过滤); `blocks="all"`; 每块 results 只含全落在块内的 qubits / subsystems;
  块删掉块外一切面(含爪)→ 对地项偏低, 指向 physics §13。
- §4.1: `templates` 省略 = 版图目录, 查找 `<dir>/<name>.yaml` 与 `<dir>/<name>/<name>.yaml`; `ground` 省略 = `none`, `sheet` 可列表, 矩形 = 该层 **metal** 面 bbox;
  手写步骤的 `layers: <芯片层 id>`(蚀刻面所在层, 多层时必填)、端口变量注入名 `<实例>_<端口>_x`、手写端口名扁平唯一、`net` 只在一层。
- §4.2: 模板顶层键与各段合法键(`_ENTRY_KEYS`)成表; 端口 `layer` 默认第一个槽位、`leq` 默认 0、多岛不写 `island:` raise; `if` 端口与外挂面必须同步;
  `params` 必须有限数、别叫 `on`/`off`(YAML 1.1); `.geo` 侧约定单列一段(输出变量形态、`_` 前缀、`Error` 守卫、面表可多面)。
- 新增 §4.4 生成的 Physical 名一览(岛 / 外挂 / 路由体 / 结 / 地), §4.5 嵌套再导出(原文引到不存在的"5.1"; 带 `.geo` 的模板不支持再导出),
  §4.6 `Layout` 记账字段(qubits / subsystems 条目的精确形态、inputs 含递归 Include)。
- §5 常见坑 10 → 17 条(`Call` 独占一行、YAML 布尔、连接型 `at/rot/mirror: y`、`templates` 漏 lib、`if` 未声明、外挂面没人连、`build(blocks=)`)。
- 表格里 `quarter_wave|half_wave` 的竖线转义(原文就有, GitHub 会切列)。

## 顺手发现, 未改 (代码)
- `layout.py` `_geo_step`: 手写步骤 `layers: <id>` 指定蚀刻层时**不校验** id 在 `meta.layers` 里 —— 拼错的层名会让该蚀刻面在 `_ground` 里被当"没建地的层"
  静默丢弃, 地上少一个孔而不报错。一行 `self._chip_layer(layer, …)` 可堵。属于 CLAUDE.md "拒绝静默"范畴, 本会话只改文档, 留给下一次。

## 状态
`status.md` headline 不变(纯文档)。未提交。

## 追问: 「meta 层面同时用 layout 和 .geo 手写微调」
- 核实: **不行**。`load_meta` 两个都给 → `give exactly one of 'geo' / 'layout'`(实跑 + `test_meta_layers_table` 已锚)。手写微调的入口是版图**内**的 `geo:` 步骤。
- 实跑 `frame: Q00` 路线(仓里此前无例子无测试): scratchpad `mix/`: 3 个 disc_transmon + 2 个 bar_coupler + `{geo: readout_pad.geo, frame: Q00}`(45° 方焊盘)
  → 多一个 `metal::ta::RP00::pad`, GDS 预览位置对(E、N 爪之间), `Layout.inputs` 含手写文件; 焊盘贴盘边 → short raise(`Q00_a`/`RP00`); `frame: Q99` → raise。
- 用户: 别补负例、别在文档里讲大段, 先讲清楚实现。→ 撤掉 grammar §4.1 刚加的「手写微调例子」整块; 只留 §1 / §3.2 一句「meta 只认一个入口, 微调写成版图 `geo:` 步骤」
  和 §4.1 例子文件名改成真实的 `xmon_readout_launch.geo`。claude-mem 拉到的出处: 9/13 P37 用户心智模型 = 「模板 yaml+geo (layer 1,2,3) / 调用 yaml+geo (layer a..e) /
  调用 yaml 定层映射」; 9/14 P45 问「用到 .geo 会怎么呈现」, 当时 demo 了 `{geo: readout_pad.geo, frame: Q00}` 但文件没进仓 (chen 头注引的就是它)。
- 未做: `frame:` 仍无测试; 没往 `examples/` 加文件(改 chen 例子会动物理口径, 单独加又没人跑)。若要 meta 级 `geo:` + `layout:` 语法糖, 不建议:
  微调常要排在路由步骤**之前**(声明端口 / 蚀刻), meta 级只能追加在末尾, 表达不了顺序; 两套入口做同一件事。

## 追加: lib 文件语法进 grammar + examples/README 以 lib 为重心
- grammar §4.2 重写成「模板文件」7 小节 (4.2.1 pad 两文件原样最小完整例 / 4.2.2 YAML 接口键表 / 4.2.3 `.geo` 侧变量协议表 + 规则 /
  4.2.4 xmon 放置型全功能 / 4.2.5 cpw_meander + bar_coupler 连接型 / 4.2.6 disc_transmon 多岛 + 可选外挂 / 4.2.7 cpw_macros 宏库文件骨架)。
  全部用仓内真实文件, 不写长段解释。文档 532 行。
- examples/README.md 重写: 主表 `lib/` 行改指针; 新增「`lib/` 模板库」节 (5 模板 + 宏库一张表: 类型 / 岛命名 / 外挂端口 / 结 / 参数 / 用在哪;
  用法四行 yaml; 层 / 结 / 外挂三条; 「写自己的模板抄哪个」对照 grammar 小节); chen 行过期的「要自己 for_block」改成 `build(blocks=["H01"])`, 跑法加第 4 条。
