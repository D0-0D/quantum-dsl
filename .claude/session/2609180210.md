# 2609180210 — 自动布线 cpw_route (subagent) · Chen 2025 对标几何上云 (c24a1) · 3×3 整片解内存试验

## 任务 (用户, 02:10, 无人值守, 07:05 兜底 cron 唤醒)
1. 编排 subagent 写自动布线代码 (plan「方向 2」, 首次立项)。
2. 上服务器算 Chen 2025 **与论文对标的几何** (不是 3×3), 能直接与论文参考值印证; 先派 Explore 取证。用完关机。
3. 3×3 整体解也试, 看怎么降内存参数。

## 进程
- 02:15 cron 一次性 07:05 兜底已设。`@on` 开远端; 平台 16 台全 Stopped → `rrs on c24a1 -w` (64c/128G, 24G 增, Palace 0.16 spack 在 `/root/spack/.../palace-0.16.0-*/bin/palace`; 无 gmsh/conda → 网格一律本机出、`rr -p` 推)。
- 02:18 派 subagent A (general-purpose): `cpw_route` = Dubins CSC + 直段换蛇形, `route.py` + `planner: cpw` 模板 + `region:` 步骤键 + tests/test_route.py + docs/design/auto-route.md。
  派 subagent B (Explore): SI §D 仿真口径原文 / 哪种几何对哪个参考值 / 仓内能力 / 规模-内存数据。
- 02:20 本机后台出整片 100/4 网格 (`scratchpad/full3x3/mesh_full.py`); 远端脚本 `run_palace.sh` (5 s 采样 palace RSS 之和 + free, 报峰值) 推到 `c24a1:/root/qdsl/`。
- 02:27 H01 块 (75 万 tets, 105 万未知量) 推到 c24a1, np=32 跑作「GB / 百万未知量」标定 + 管线打通。
- Palace 0.16 文档核: `PartialAssemblyOrder` 默认 1 (p=2 层已是 PA), `Linear.MGMaxLevels` 100, `MGCoarsenType` Logarithmic, `EstimatorMG` false (估计器用 Jacobi), `AMGAggressiveCoarsening` false。
- 02:33 H01 标定 (c24a1 np=32): 93 s (本机 8 rank 573 s), 峰值 10.4 GB / 1.05 M 未知量 ≈ **9.9 GB/M**; Palace 0.16 末尾自报 "Estimated peak per-rank memory" 10.5 G 与采样一致 (可当预算依据; 开头的 current memory 不行)。
- 02:31 整片 3×3 100/4 本机网格 **399 s, 5.82 M tets (不是此前估的 ~9 M), msh 340 MB**; 粗档 150/6: 92 s, 2.00 M tets, 116 MB。
- 02:36 Explore 回报: 推荐「十字」= Q11 (4 爪) + 4 邻居比特 (各 1 爪) + 4 耦合器 = 18 导体 / 9 结, 一次解同时给 (a) 中心比特真晶格 C_Σ ↔ Table SI 实测 α −192 ± 6 MHz (12 只),
  (b) 臂比特 = SI §D 孤立 QCQ 比特侧口径; SI §D 关于仿真只有一句 "We numerically calculate the QCQ system capacitance from electromagnetic simulations" (无工具 / 无 d / 无边界 / 无包含范围);
  SI 不给比特编号 ↔ 晶格位置; 读出结构估 C_Σ +3%, 不画。旧 scratchpad SI 文本已不在, SI PDF 从 Springer ESM 重取。
- 02:40 写 `examples/chen_2025_cross.{layout,meta}.yaml` (9 步, 无 extract), GDS PNG 目检手性正确; 网格 100/4 本机 107 s, **2.39 M tets, 137 MB**; 测试 `test_chen_2025_cross_is_one_four_coordinated_qubit_solved_whole` 加进 test_chen_2025.py。
- 02:37 subagent A 交付: `route.py` (Dubins CSC + 蛇形) / `lib/cpw_route` / `cpw_route_demo` / `tests/test_route.py` 4 条 / `docs/design/auto-route.md` / grammar / SPEC / README / architecture; 本机复跑 **43 passed / 2 skipped** (+ 十字 1 条 = 44)。
  代理自报的待拍板: ① demo 不是 pad↔pad (两岛端无 net 名规则); ② 自动腿数取最小 n → 稀疏大振幅, 若要紧凑改取最大 n (一行); ③ CSC-only, 两口距 < 4R 不保证最短。
- 02:41 3×3 基线 c24a1 np=32: **8 123 703 未知量 (p=1 层 1 044 989)**, 装配阶段冲到 56 GB, 求解中 43–57 GB (系统 used 57 / avail 66) → 128 G 机装得下, 与 8.3–9.9 GB/M 经验值一致。
- 02:46 十字 np=24 与 3×3 并发起跑 (估 +30 GB)。
- 02:55 十字解 (c24a1 np=24, 与 3×3 并发): **3 343 345 未知量**, ~50 s / terminal, 常驻 27.7 GB; 3×3 并发下 123 s / terminal, 65.4 GB (两任务合计 91 GB / 125 GB)。
  十字 d=7 网格本机 102 s (2.39 M tets) 已推 c24a1:/root/qdsl/cross_d7; 80/3 第二档在本机出网格中。采样脚本改 `run_palace2.sh` 按 config 名分任务统计 RSS (原版把两任务加在一起)。
- 03:02 本地 CC 进程重启过一次 (会话 resume): 远端 setsid job 未受影响 (十字 17/18、3×3 10/42 照跑), cron 与 `@` 路由仍在; 旧 scratchpad 被清 (本机 msh 副本与 postpro.py 丢, 网格全在 c24a1, 脚本重写)。
- **03:05 十字 d=5 100/4 解完 (c24a1 np=24, 与 3×3 并发): 1140 s, Palace 自报峰值 28.6 GB** (≈ 8.6 GB / M 未知量)。产物 `.claude/chen-evidence/cross_d5_100_4_*`。
  | 比特 | 爪数 | C_Σ (fF) | E_C = −α (MHz) | 参考 |
  |---|---|---|---|---|
  | 中心 Q11 | 4 | 90.31 | **214.5** | 实测 α −179～−200 (均值 −192 ± 6); SI 网络自身闭合 209; 设计 −185 |
  | 臂 Q01/Q21/Q10/Q12 | 1 | 88.08–88.09 | 219.9 | = 此前 H01 块解 220 (块解 = SI 孤立 QCQ 口径, 数值到 0.1 MHz 重合 → 块删爪的偏差只体现在**多爪比特**上) |
  | 耦合器 ×4 | — | 125.6 | 154.2 | SI 闭合 171, 块解 154 |
  g_qc 64.5–66.7 MHz (SI 闭合 71, 设计 90), g_qq (直接) 3.70 MHz (SI 闭合 9.1, 设计 5), 耦合器–耦合器 0.7–6.5 MHz。
  读法: ① 四配位只把 E_C 从 220 压到 214.5 (−2.5%), 比 Explore 预估 (198–208) 小得多 —— 多出来的 3 只爪主要把半盘的场从「对地」改道成「对耦合器互容」(Q11 半盘对地 133 vs 臂 145/153), C_Σ 净增只有 2.2 fF;
  ② 与实测 −192 的缺口 12% 是**整个口径**的缺口 (SI 自己的网络也只给 209), 不是环境效应; d 越大 E_C 越高 (块解 d=7 → 259), 所以要靠 d 标定只能往 **d ≈ 4 µm** 走 (E_C ∝ d^0.49 外推), 已出 d=4 网格; 读出结构估 +3% C_Σ 也是同方向但不够。
  ③ 互容 (半盘–半盘 10.2 / 半盘–条 12.7 / 条–板 10.7 fF) 与块解逐位相同 → 互容 ×0.4–0.5 的缺口纯是几何 (缝 / 间隙), 与环境无关。
- 03:08 十字 d=7 起跑 (np=24, `run_palace2.sh` 分任务采样)。
- 03:12 队列化 (远端 setsid job, 免疫本地会话): ① d=7 完 → d=4 (np=24); ② 3×3 基线完 → 十字 80/3 (np=32, 估 60 GB) → 3×3 np=16 只测峰值 (`run_peak.sh`: 出现 "It 3/" 即杀) → 3×3 `AMGAggressiveCoarsening: true` 只测峰值 → 3×3 粗档 150/6 (2.0 M tets) 全解。
  预计 ~05:40 全部结束; 07:05 cron 兜底。
- **03:27 十字 d=7 解完** (1123 s, 峰值 28.1 GB, 产物 `cross_d7_100_4_results.yaml`): 中心 Q11 C_Σ 77.34 / E_C **250.4** MHz (d=5: 90.31 / 214.5), 臂 Q01 74.78 / 259.0 (= 块解 d=7 259), 耦合器 E_C 180.6, g_qc 84–85 MHz, g_qq 5.8 MHz。
  E_C ∝ d^0.46 (十字, 两点拟合) → 命中实测 α −192 需 **d ≈ 3.9 µm**; d=4 已在跑。互容 (11.3 / 13.5 / 12.2) 随 d 只动 +6–14%, 仍是 SI 的一半。
- **03:47 十字 d=4 解完** (1158 s, 28.3 GB, `cross_d4_100_4_results.yaml`): 中心 Q11 C_Σ 101.57 fF / **E_C 190.7 MHz → α −190.7 MHz, 实测 12 只均值 −192 ± 6 (范围 −179～−200), 命中**;
  f01 = 4.159 GHz (E_J 用 SI 设计值 12.4 GHz; 实测 f_max 4.03–4.25); 臂比特 194.5; 耦合器 E_C 137.2 / f01 4.675 GHz; g_qc 55.4–55.7 MHz (设计 90, SI 闭合 71), g_qq 2.6 MHz (设计 5)。
  互容 9.5 / 12.3 / 9.7 fF (仍 ≈ SI 的 0.4 倍)。三点 d = 4 / 5 / 7 → E_C 190.7 / 214.5 / 250.4 (中心), ∝ d^0.49。
  ⚠ 单网格 (100/4) 未收敛; 未画读出爪 (估 C_Σ +3% → 等效 d 再 +0.3 µm ≈ 4.3); d 的外部锚 5 ± 0.4 (BAQIS 工艺) 与本标定 4.0 差 2.5σ —— 与 SI 自身网络 (E_C 209 ↔ d ≈ 5.2) 一致的口径缺口, 不是环境效应。
- **04:05 3×3 整片解完** (c24a1 np=32, 5158 s = 86 min, Palace 自报峰值 **69.2 GB**, 与十字并发时系统 used 94 GB; 产物 `full3x3_d5_100_4_*`): 42 导体 / 21 结。
  比特 E_C 按配位数: 角 (2 爪) 218.0–218.1, 边 (3 爪) 216.3, 中心 Q11 (4 爪) **214.5 = 十字解到 0.1 MHz** → 十字对比特级量已足够, 3×3 只多出阵列级耦合;
  每多一只爪 E_C −1.8 MHz。耦合器 21 只全部 154.2 MHz。g_qc 64.2–67.6 MHz; 最近邻直接 g_qq 3.65–3.70 MHz (设计 5); **对角次近邻 g 0.03–0.05 MHz**（半盘–半盘互容 0.0007 fF）
  → SI §G 实测的 ~2 MHz 次近邻残余耦合不是静电直接项, 是经耦合器 / 频率邻近的间接项; 相邻耦合器 (H01–V10) 互容 0.36 fF, g_cc 4.8–6.5 MHz。
- **04:36 十字 d=5 80/3 解完** (np=32, 1405 s, **6.94 M 未知量, 峰值 59.0 GB**; `cross_d5_80_3_results.yaml`): 对 100/4 —— 对地 C −0.1～−0.3%, 互容 −0.5～−1.1%, **E_C +0.3%**, g_qc −0.6%, g_qq −1.3%。
  两档网格差 ≪ 与实测的 12% 缺口和 d 三点 (191/215/250) 的间距 → d = 4 命中的结论不受网格影响 (80/3 下 d=4 的 E_C 估 191.3)。
- **05:20 队列结束, 3×3 降内存对照 (同一张 100/4 网格 812 万未知量, 除注明外 np=32)**:
  | 变体 | 峰值内存 | 每 terminal 用时 | 备注 |
  |---|---|---|---|
  | 基线 100/4 np=32 | **69.2 GB** (Palace 自报; 采样 65–67 GB 常驻) | 123 s (与十字并发时) | 42 terminal 全解 86 min |
  | np=16 | 63.4 GB (−8%) | 213 s (×1.7) | 只测峰值 (It 3 出现即杀); 省的内存不值时间 |
  | `Linear.AMGAggressiveCoarsening: true` | 65.6 GB (−5%) | 119 s (同) | 只测峰值; 边际 |
  | **网格 150/6** (2.00 M tets / 282 万未知量) | **25.7 GB (÷2.7)** | 47 s (÷2.6) | 全解 32 min; 对 100/4: 对地 C +0.2～0.7%, 互容 +1～2%, **E_C −0.5～−0.6%**, g_qc +1% |
  结论: Palace 0.16 默认已是 PA (`PartialAssemblyOrder` 1), 求解器侧旋钮 (rank 数 / AMG 粗化) 只有 5–8%; **内存杠杆在网格 `min_size_um`** (4 → 6 就 ÷2.7, 精度代价 <1% E_C / ~2% 互容)。
  经验值刷新: 8.1–8.6 GB / 百万未知量 (order 2, np 24–32; 十字 3.34 M → 28.6 G, 6.94 M → 59.0 G, 3×3 8.12 M → 69.2 G, 2.82 M → 25.7 G); 128 G 机能装 ~14 M 未知量。
- 05:22 全部产物拉回 (`.claude/chen-evidence/` 6 份 results + summaries), `rrs off c24a1`。机时: 02:17–05:22 ≈ 3 h × 50 灵豆/h。

## 07:06 兜底 cron 核对 (用户预设)
- ① 自动布线: `route.py` / `lib/cpw_route` / `test_route.py` / `docs/design/auto-route.md` 在工作树; 全套 **44 passed / 2 skipped** (43 s)。
- ② Chen 对标几何: 十字 d=4/5/7 + 80/3、3×3 100/4 + 150/6 共 6 份 results 与运行摘要都在 `.claude/chen-evidence/`; 平台 16 台全部 Stopped (c24a1 05:22 关, 无残留 job)。
- ③ 3×3 内存试验有结论 (physics §14: 杠杆在 `min_size_um`, 求解器旋钮 5–8%)。
- 无异常中断; status.md / plan.md / physics / SPEC / README 均已在 05:30 前刷新。**未提交** (用户未要求)。
