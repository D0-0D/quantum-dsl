# 2026-09-24 12:10 — open issue 清理: #25 关, #23 / #26 落地

- #25 Elmer 后端: 关闭（评论指 physics §12 结案账 + `.claude/xmon-evidence/traced_160-10_elmer_cap.sif`）。
- #23 `targets:`: meta.py `_targets` 展平 + 严格校验（tol 只收 "8%", 字段名 = results 数值字段, 值裸数非零）;
  build.py `_check_target_names`（网格化前）+ `_validation`（整片 results 的 `validation` 段, 未命中 warn）。
  sung meta 文末加 targets, `test_sung_2021_against_paper` 改读 validation 并锁死核对集合与容差（未上 384G 机实跑）。
- #26 `converge(meta, out, scales)`: `dataclasses.replace` 缩放 mesh 两尺寸, 每档 `build(solve=True)` → `mesh_x<s>/`,
  `convergence.yaml`。实测 two_pads (2,1): 细档 = golden 逐位; C_AA −1.547% / C_BB −1.652% / C_AB +3.026%。
- results.yaml / convergence.yaml 改 `allow_unicode=True`（source 里的中文）。
- 测试 51 → 54 passed / 3 skipped（live 加 `test_two_pads_converge`）。
